# audio
Audio utilities

## Features
- Stream audio data in PCM 16-bit format.
- Extract loudness from an audio stream at a given frequency, with volume averaging.

## Tests
- A test generates a sine wave (1 second) followed by silence, repeated 4 times, and extracts the loudness as a list of integers.
